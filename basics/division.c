#include <stdio.h>

int main()
{
    int a = 21;
    int b = 10;

    printf("21 when divided by 10 leaves the remainder %d\n", a % b);
    // Expected output: 21 when divided by 10 leaves the remainder 1

    /* Explanation: The modulus operator (%) gives the remainder of the
    division of a by b. */
    
    return 0;

}